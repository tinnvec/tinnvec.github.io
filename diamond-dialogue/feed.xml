---
layout: null
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom"{% if site.lang %} xml:lang="{{ site.lang }}"{% endif %}>
  <generator uri="http://jekyllrb.com" version="{{ jekyll.version }}">Jekyll</generator>
  <link href="{{ page.url | absolute_url | prepend: site.url }}" rel="self" type="application/atom+xml" />
  <link href="{{ '/diamond-dialogue' | absolute_url | prepend: site.url }}" rel="alternate" type="text/html" {% if site.lang %}hreflang="{{ site.lang }}" {% endif %}/>
  <updated>{{ site.time | date_to_xmlschema }}</updated>
  <id>{{ '/' | absolute_url | prepend: site.url | xml_escape }}/</id>

  {% if site.title %}
    <title type="html">{{ site.title | smartify | xml_escape }}</title>
  {% elsif site.name %}
    <title type="html">{{ site.name | smartify | xml_escape }}</title>
  {% endif %}

  {% if site.description %}
    <subtitle>{{ site.description | xml_escape }}</subtitle>
  {% endif %}

  {% if site.author %}
    <author>
        <name>{{ site.author.name | default: site.author | xml_escape }}</name>
      {% if site.author.email %}
        <email>{{ site.author.email | xml_escape }}</email>
      {% endif %}
      {% if site.author.uri %}
        <uri>{{ site.author.uri | xml_escape }}</uri>
      {% endif %}
    </author>
  {% endif %}

  {% for episode in site.diamond-dialogue %}
    <entry>
      <title type="html">{{ episode.title | smartify | strip_html | normalize_whitespace | xml_escape }}</title>
      <link href="{{ episode.url | absolute_url | prepend: site.url }}" rel="alternate" type="text/html" title="{{ episode.title | xml_escape }}" />
      <published>{{ episode.date | date_to_xmlschema }}</published>
      <updated>{{ episode.last_modified_at | default: episode.date | date_to_xmlschema }}</updated>
      <id>{{ episode.id | absolute_url | prepend: site.url | xml_escape }}</id>

      <link rel="enclosure"  type="audio/mpeg" title="MP3" href="{{ episode.audio.file }}" length="{{ episode.audio.bytes }}" />
      <link rel="enclosure" type="video/mp4" title="MP4" href="{{ episode.video.file }}" length="{{ episode.video.bytes }}" />

      <content type="html" xml:base="{{ episode.url | absolute_url | prepend: site.url | xml_escape }}">{{ episode.description | strip | xml_escape }}</content>

      {% assign post_author = episode.author | default: episode.authors[0] | default: site.author %}
      {% assign post_author = site.data.authors[post_author] | default: post_author %}
      {% assign post_author_email = post_author.email | default: nil %}
      {% assign post_author_uri = post_author.uri | default: nil %}
      {% assign post_author_name = post_author.name | default: post_author %}

      {% if post_author %}
      <author>
          <name>{{ post_author_name | xml_escape }}</name>
        {% if post_author_email %}
          <email>{{ post_author_email | xml_escape }}</email>
        {% endif %}
        {% if post_author_uri %}
          <uri>{{ post_author_uri | xml_escape }}</uri>
        {% endif %}
      </author>
      {% endif %}      
    </entry>
  {% endfor %}
</feed>